# Cloudflare Pages — path-level rewrites and redirects.
# Cross-host redirects (www→apex, http→https) handled in Cloudflare dashboard:
#   - SSL/TLS → Edge Certificates → "Always Use HTTPS" ON
#   - Rules → Redirect Rules → www.aleksandarpavlov.com → aleksandarpavlov.com (301)

# Clean URL rewrites for static pages
/about                /about.html                200
/forma-architects     /forma-architects.html     200

# NOTE: do NOT add ".html → /clean-url" 301 rules here. They form a loop
# with the rewrites above on Cloudflare Pages, producing ERR_TOO_MANY_REDIRECTS.

# Dynamic project pages (JS-rendered, noscript fallback included)
/birse                /project.html?slug=birse                200
/creative-asset       /project.html?slug=creative-asset       200
/swis-saas            /project.html?slug=swis-saas            200

# NOTE: do NOT add a catchall like "/*  /404.html  404" on Cloudflare Pages —
# it is processed before static asset lookup and returns 404 for every image/CSS/JS.
# Cloudflare Pages automatically serves /404.html for unmatched routes when present.
